package io.grasscutter.utils.constants;

/* Constants seen in file-scoped classes. */
public interface FileConstants {
    // These are the file extensions used for game resources.
    String[] RESOURCE_EXTENSIONS = {"tsj", "json", "tsv"};
}
