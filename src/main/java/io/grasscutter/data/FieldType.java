package io.grasscutter.data;

/** The type of field. Used with {@link Special}. */
public enum FieldType {
    /**
     * This field is an ID. In relational databases with a primary key, this is the primary key.
     */
    ID
}